name: Release

# Conforms to the umbrella SDK release pipeline contract:
# u5c-factory reference/sdk-pipeline.md
on:
  push:
    tags: ['v*']

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify tag matches package version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          MANIFEST=$(sed -nE 's/.*<Version>(.+)<\/Version>.*/\1/p' src/Utxorpc.Sdk/Utxorpc.Sdk.csproj)
          if [ "$TAG" != "$MANIFEST" ]; then
            echo "::error::tag $GITHUB_REF_NAME does not match Utxorpc.Sdk.csproj version $MANIFEST"
            exit 1
          fi

  build:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0"
      - run: dotnet restore
      - run: dotnet build --no-restore --configuration Release

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0"
      - run: dotnet restore
      # Utxorpc.Sdk.Test is a live-server suite; run only Category=Unit (see ci.yml).
      - run: dotnet test --filter "Category=Unit"

  publish:
    needs: test
    runs-on: ubuntu-latest
    # Required for NuGet trusted publishing: the job mints a GitHub OIDC token
    # that nuget.org exchanges for a short-lived key. No long-lived secret.
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0"
      - name: Pack
        run: >-
          dotnet pack src/Utxorpc.Sdk/Utxorpc.Sdk.csproj
          --configuration Release
          --output ./nupkgs
      # Issued keys live for one hour and are single-use, so this runs
      # immediately before the push rather than at the top of the job.
      - name: NuGet login
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}
      - name: Publish to NuGet
        run: >-
          dotnet nuget push ./nupkgs/*.nupkg
          --source https://api.nuget.org/v3/index.json
          --api-key ${{ steps.login.outputs.NUGET_API_KEY }}
          --skip-duplicate
